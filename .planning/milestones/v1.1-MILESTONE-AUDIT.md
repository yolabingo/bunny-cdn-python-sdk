---
milestone: v1.1
name: Reliability & Coverage
audited: 2026-04-10T00:00:00Z
status: tech_debt
scores:
  requirements: 8.5/9
  phases: 3/3
  integration: 3/3
  flows: 4/4
gaps:
  requirements:
    - id: QUAL-02
      status: partial
      phase: 05-quality-and-coverage
      description: >
        Context manager cleanup path (aclose() when _client_owner=True) covered by lifecycle tests.
        However _client.py reached 93%, not 100% — lines 121-122 (bare except Exception in JSON
        error extraction) remain uncovered. Functional goal met; metric target missed.
      claimed_by_plans: [05-02-PLAN.md]
      completed_by_plans: [05-02-SUMMARY.md (partial)]
      verification_status: missing
      evidence: >
        test_client_lifecycle.py covers __aenter__/__aexit__/__enter__/__exit__ with
        _client_owner=True, exercising the aclose() call at line 83. Lines 121-122 require
        a response where .json() raises during error extraction — no test simulates this.
        Lines 133/135 (5xx raise branch) are covered by test_exceptions.py::test_server_error
        in the full suite.
    - id: QUAL-04-minor
      status: partial
      phase: 05-quality-and-coverage
      description: >
        test_public_surface.py smoke tests CoreClient, StorageClient, BunnyAPIError but does
        not explicitly import RetryTransport (added in Phase 06). RetryTransport is effectively
        covered by test_retry.py imports (22 tests), but not part of the named public surface test.
      claimed_by_plans: [05-01-PLAN.md]
      completed_by_plans: [05-01-SUMMARY.md]
      verification_status: missing
      evidence: >
        test_public_surface.py::test_public_imports asserts CoreClient, StorageClient,
        BunnyAPIError — but RetryTransport (exported in __all__ as of Phase 06) is absent
        from that test.
tech_debt:
  - phase: all
    items:
      - "No VERIFICATION.md files created for phases 05, 06, 07 — only SUMMARY.md self-checks + UAT"
  - phase: 05-quality-and-coverage
    items:
      - "_client.py lines 121-122 uncovered: bare except Exception in JSON error-message extraction
        (requires a 4xx/5xx response where response.json() itself raises — non-JSON malformed body)"
  - phase: 06-retrytransport
    items:
      - "2 pre-existing ty errors in storage.py (call-non-callable, invalid-type-form) deferred — not introduced by Phase 06"
  - phase: 01-04 (v1.0 carried forward)
    items:
      - "ruff baseline had 59 existing violations (PLR2004, ANN401, TRY003) — deferred, not introduced by v1.1"
nyquist:
  overall: disabled
  note: workflow.nyquist_validation is false
---

# Milestone v1.1 Audit: Reliability & Coverage

**Audited:** 2026-04-10
**Status:** ⚡ Tech Debt (no blockers)
**Score:** 8.5/9 requirements | 3/3 phases | 4/4 E2E flows

---

## Requirements Coverage

| Req ID | Description | Phase | Status | Evidence |
|--------|-------------|-------|--------|----------|
| QUAL-01 | `BunnyAPIError.__str__` covered | 05 | ✅ satisfied | `assert str(exc_info.value) == "HTTP 401: error"` in test_exceptions.py; `_exceptions.py` at 100% |
| QUAL-02 | Context manager cleanup path + `_client.py` 100% | 05 | ⚠ partial | Lifecycle paths covered; `_client.py` at 93% (lines 121-122 uncovered) |
| QUAL-03 | `list_single_page()` removed from `_pagination.py` | 05 | ✅ satisfied | Removed in commit 0e0e966; `_pagination.__all__` contains only `pagination_iterator` |
| QUAL-04 | Public surface smoke test | 05 | ✅ satisfied | `test_public_surface.py::test_public_imports` confirms CoreClient, StorageClient, BunnyAPIError |
| RETRY-01 | `from bunny_cdn_sdk import RetryTransport` | 06 | ✅ satisfied | `__init__.py` exports in `__all__`; 22 tests import it |
| RETRY-02 | Retry on 429/5xx/ConnectError/TimeoutException | 06 | ✅ satisfied | `_retry.py::_should_retry_response` + exception catch; 8 trigger tests pass |
| RETRY-03 | Exponential backoff with jitter + constructor params | 06 | ✅ satisfied | `_backoff_delay` uses `random.uniform(0, min(60.0, base * 2**attempt))`; `test_backoff_grows_exponentially` passes |
| RETRY-04 | Constructor kwargs `max_retries=0` / `backoff_base=0.5` | 07 | ✅ satisfied | All three clients wired; 15 integration tests; `max_retries=0` → 1 HTTP call confirmed |
| RETRY-05 | RetryTransport independently composable | 06 | ✅ satisfied | Extends `httpx.AsyncBaseTransport`; `test_composability_with_async_http_transport` passes |

**Summary:** 8 satisfied, 1 partial (QUAL-02), 0 unsatisfied

---

## Phase Verification Status

| Phase | VERIFICATION.md | SUMMARY self-check | UAT | Overall |
|-------|----------------|--------------------|-----|---------|
| 05-quality-and-coverage | ❌ missing | ✅ PASSED | ✅ (phases 05-07 in 07-UAT.md) | ⚠ no formal VERIFICATION |
| 06-retrytransport | ❌ missing | ✅ PASSED | ✅ | ⚠ no formal VERIFICATION |
| 07-constructor-integration | ❌ missing | ✅ PASSED | ✅ 8/8 tests passed | ⚠ no formal VERIFICATION |

No VERIFICATION.md files were created during this milestone (workflow ran plan → execute → UAT path without a `/gsd-verify-work` per-phase step). All phase SUMMARY.md files include "Self-Check: PASSED" sections with explicit command output.

---

## Cross-Phase Integration

### Wiring Map

```
Phase 06: _retry.py (RetryTransport)
    └──▶ Phase 07: _client.py imports RetryTransport
             └──▶ _BaseClient.__init__: auto-wires when max_retries > 0
                      └──▶ CoreClient.__init__ → passes max_retries/backoff_base to super()
                      └──▶ StorageClient.__init__ → passes max_retries/backoff_base to super()
    └──▶ __init__.py: exports RetryTransport in __all__

Phase 05: removes list_single_page from _pagination.py
    └──▶ No consumers in Phase 06/07 — clean removal, no interference
```

**Result:** All cross-phase wiring correct. No orphaned exports. No missing connections.

---

## E2E Flow Verification

| Flow | Status | Evidence |
|------|--------|----------|
| `from bunny_cdn_sdk import CoreClient, StorageClient, RetryTransport` | ✅ complete | `__init__.py` exports all three; test_public_surface + test_retry exercise |
| `CoreClient(api_key, max_retries=3)` → 5xx → retry → success | ✅ complete | `test_core_client_max_retries_1_succeeds_on_second_call`; full call chain verified |
| `StorageClient(zone, pwd, max_retries=2)` → retry on 5xx | ✅ complete | `test_storage_client_max_retries_1_produces_2_calls` |
| User-composed `httpx.AsyncClient(transport=RetryTransport(...))` → inject via `client=` | ✅ complete | `test_composability_with_async_http_transport`; `_BaseClient` takes `client is not None` branch |

---

## Tech Debt Summary

### From v1.1

1. **`_client.py` lines 121-122 uncovered** — bare `except Exception` in JSON error-message extraction requires a malformed response body that fails `.json()` and `.text` access. Low-risk edge case. Cover in future test cycle.
2. **No VERIFICATION.md for phases 05-07** — procedural gap; quality confirmed via SUMMARY self-checks and full UAT. Run `/gsd-validate-phase 5 6 7` if formal verification records are needed.
3. **`test_public_surface.py` missing RetryTransport** — minor; add `RetryTransport` to the smoke test in the next cleanup cycle.

### Carried from v1.0

4. **Pre-existing ty errors in `storage.py`** — `call-non-callable` and `invalid-type-form`; not introduced by v1.1.
5. **59 pre-existing ruff violations** — PLR2004, ANN401, TRY003; not introduced by v1.1.

---

## Test Suite

```
98 passed in 0.23s (at audit time)
Total coverage: 99% (340 stmts, 2 miss)
_retry.py: 100%
_exceptions.py: 100%
_pagination.py: 100%
core.py: 100%
storage.py: 100%
_client.py: 93% (lines 121-122 remain)
```

---

## Verdict

**⚡ Tech Debt — No Blockers**

All 9 v1.1 requirements are functionally satisfied. QUAL-02 metric target (`_client.py` to 100%) is not met — 4 uncovered lines remain, but the stated functional goal (context manager cleanup path covered) is achieved. No cross-phase wiring defects. All 4 E2E flows complete. 98 tests pass.

The milestone is shippable. The QUAL-02 coverage gap and missing VERIFICATION.md files are the only items worth tracking.

---

*Audit performed: 2026-04-10 | Integration check by: gsd-integration-checker*
