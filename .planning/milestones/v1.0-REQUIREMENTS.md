# Archived Requirements: bunny-cdn-sdk v1.0

**Archived:** 2026-04-10
**Outcome:** 29/29 requirements satisfied

---

## Core Value

A Python developer can `pip install bunny-cdn-sdk`, instantiate a client with their API key, and call methods that map 1:1 to Bunny CDN endpoints — no surprises, no hidden behavior.

**Validated:** All core value criteria met as of v1.0.

---

## v1 Requirements — Final Status

### SDK Foundation

- [x] **INFRA-01** — Base client wraps `httpx.AsyncClient` with sync public surface via `asyncio.run()`
  - *Validated: `_client.py` implements async internals with `_sync_request()` wrapping `asyncio.run()`*
- [x] **INFRA-02** — `_request()` chokepoint injects `AccessKey` header and `User-Agent: bunny-cdn-sdk/{version}` on every call
  - *Validated: header injection confirmed in `_client.py` `_request()`; tested via MockTransport*
- [x] **INFRA-03** — `_gather()` runs multiple coroutines concurrently via `asyncio.gather()` and returns results as a list
  - *Validated: `_gather()` implemented; `get_pull_zones` concurrent test passes*
- [x] **INFRA-04** — Clients support context manager protocol (`with` statement)
  - *Validated: sync + async context managers implemented; cleanup path manually verified in Phase 2*
- [x] **INFRA-05** — Clients accept optional `httpx.AsyncClient` for testing and custom transports
  - *Validated: constructor accepts `client` kwarg; injection pattern used in all 58 tests*
- [x] **INFRA-06** — Exception hierarchy: `BunnySDKError` → `BunnyAPIError` (401/404/429/5xx) + `BunnyConnectionError` → `BunnyTimeoutError`
  - *Validated: 8 classes; all 6 exception types triggered in `test_exceptions.py`*
- [x] **INFRA-07** — `PaginatedResponse` TypedDict with `Items`, `CurrentPage`, `TotalItems`, `HasMoreItems`
  - *Validated: TypedDict implemented in `_types.py`; used in `iter_*` tests*
- [x] **INFRA-08** — `list_*()` single-page fetch; `iter_*()` auto-paginating iterator — pagination decoupled via callback
  - *Validated: both patterns implemented; `iter_pull_zones` 2-page test passes*
  - *Note: `list_single_page()` helper exported but unused — `list_*` methods call `_sync_request()` directly (tech debt)*
- [x] **INFRA-09** — `py.typed` PEP 561 marker present in package
  - *Validated: `src/bunny_cdn_sdk/py.typed` 0 bytes; `ty` discovers it*
- [x] **INFRA-10** — `__init__.py` re-exports `CoreClient`, `StorageClient`, and all exception classes
  - *Validated: direct runtime imports in place after post-audit fix (commit `5b57a66`)*
  - *History: Phase 2 used TYPE_CHECKING guard as forward reference; Phase 3 implemented modules but guard remained; fixed in quick task 260410-p0m*

### Core API

- [x] **CORE-01** — `CoreClient` Pull Zone CRUD — `list_pull_zones`, `get_pull_zone`, `create_pull_zone`, `update_pull_zone`, `delete_pull_zone`
- [x] **CORE-02** — `CoreClient` Pull Zone extras — `purge_pull_zone_cache`, `add_custom_hostname`, `remove_custom_hostname`, `add_blocked_ip`, `remove_blocked_ip`
- [x] **CORE-03** — `CoreClient.get_pull_zones(ids)` fetches multiple pull zones concurrently using `_gather()`
- [x] **CORE-04** — `CoreClient` Pull Zone pagination — `list_pull_zones(page, per_page, search)` + `iter_pull_zones(per_page, search)`
- [x] **CORE-05** — `CoreClient` Storage Zone CRUD — `list_storage_zones`, `get_storage_zone`, `create_storage_zone`, `update_storage_zone`, `delete_storage_zone`
- [x] **CORE-06** — `CoreClient` Storage Zone pagination — `list_storage_zones(page, per_page)` + `iter_storage_zones(per_page)`
- [x] **CORE-07** — `CoreClient` DNS Zone CRUD — `list_dns_zones`, `get_dns_zone`, `create_dns_zone`, `update_dns_zone`, `delete_dns_zone`
- [x] **CORE-08** — `CoreClient` DNS Record management — `add_dns_record`, `update_dns_record`, `delete_dns_record`
- [x] **CORE-09** — `CoreClient` DNS Zone pagination — `list_dns_zones(page, per_page, search)` + `iter_dns_zones(per_page, search)`
- [x] **CORE-10** — `CoreClient` Video Library CRUD — `list_video_libraries`, `get_video_library`, `create_video_library`, `update_video_library`, `delete_video_library`
- [x] **CORE-11** — `CoreClient` utilities — `purge_url(url)`, `get_statistics(**kwargs)`, `list_countries()`, `list_regions()`, `get_billing()`

### Storage API

- [x] **STOR-01** — `StorageClient.upload(path, data, content_type?)` accepts `bytes | BinaryIO`
  - *Note: BinaryIO is read into bytes before passing to httpx — sync file-like objects incompatible with AsyncByteStream*
- [x] **STOR-02** — `StorageClient.download(path)` returns `bytes`
- [x] **STOR-03** — `StorageClient.delete(path)` removes a file from the storage zone
- [x] **STOR-04** — `StorageClient.list(path)` returns flat directory listing as `list[dict]`
- [x] **STOR-05** — `StorageClient` constructor maps `region` to correct base URL across all 10 regions
  - *Validated: falkenstein (default), de, ny, la, sg, syd, uk, se, br, jh all tested*

### Tests

- [x] **TEST-01** — All exception types tested via `httpx.MockTransport`
- [x] **TEST-02** — `CoreClient` methods tested with mocked responses covering all endpoints
- [x] **TEST-03** — `StorageClient` methods tested with mocked responses covering all 4 operations

---

## Out of Scope (Unchanged)

| Feature | Reason |
|---------|--------|
| Response model objects (Pydantic/dataclass) | Plain dicts avoid dependency and schema-change brittleness |
| Rate limit auto-retry on 429 | Thin wrapper — users handle retry policy |
| Webhook signature verification | Outside v1 scope |
| CLI tool | Not a goal for the SDK |
| Response caching | No magic |
| Automatic content-type detection for uploads | Caller responsibility |
| Unified client holding all API keys | Separate clients match Bunny's per-service auth model |
| `StreamClient` (Stream API) | Deferred to v2 |
| Retry logic / exponential backoff | Users can use custom httpx transports |
| File upload progress callbacks | v2 candidate |
| Shield API, Edge Scripting API, Magic Containers | Deferred to future version |

---

## v2 Requirements (Deferred)

### Stream API

- **STRM-01**: `StreamClient` Videos CRUD
- **STRM-02**: `StreamClient.upload_video(video_id, data)` streams video bytes
- **STRM-03**: `StreamClient` Collections CRUD
- **STRM-04**: Stream API pagination (`page`, `itemsPerPage` — camelCase envelope)

### Extended Coverage

- **EXT-01**: Retry logic / exponential backoff via custom transport
- **EXT-02**: File upload progress callbacks
- **EXT-03**: Shield API, Edge Scripting API, Magic Containers API

---

*Requirements defined: 2026-04-10*
*Archived: 2026-04-10 at v1.0 milestone completion*
