---
phase: 01-package-scaffold-exception-hierarchy
plan: 01
subsystem: package-scaffold
tags: [python, package, uv, ruff, scaffold]
dependency_graph:
  requires: []
  provides: [installable-package, py-typed-marker, module-stubs]
  affects: [all-subsequent-phases]
tech_stack:
  added: [uv, ruff, ty, pytest, pytest-cov, pip-audit, poethepoet, httpx]
  patterns: [src-layout, pep561-py-typed, uv-lock-committed]
key_files:
  created:
    - pyproject.toml
    - src/bunny_cdn_sdk/__init__.py
    - src/bunny_cdn_sdk/_client.py
    - src/bunny_cdn_sdk/_exceptions.py
    - src/bunny_cdn_sdk/_pagination.py
    - src/bunny_cdn_sdk/_types.py
    - src/bunny_cdn_sdk/core.py
    - src/bunny_cdn_sdk/storage.py
    - src/bunny_cdn_sdk/py.typed
    - uv.lock
  modified:
    - CLAUDE.md
decisions:
  - "Python 3.12 as minimum version floor (D-01: modern asyncio, union types, match statements)"
  - "uv.lock committed to version control for supply chain reproducibility (T-01-04)"
  - "Empty __init__.py (zero bytes) — Phase 2 will populate with re-exports"
  - "Module stubs use only module docstrings to satisfy ruff select=ALL without triggering other rules"
metrics:
  duration: "~10 minutes"
  completed: "2026-04-10"
  tasks_completed: 2
  tasks_total: 2
  files_created: 10
  files_modified: 1
---

# Phase 01 Plan 01: Package Scaffold Summary

**One-liner:** Python 3.12 package scaffold with src-layout, PEP 561 py.typed marker, and 6 empty module stubs installable via uv sync.

## What Was Built

Established the installable foundation for bunny-cdn-sdk. Every subsequent phase can now `import bunny_cdn_sdk` and find a stable module hierarchy to populate.

### Files Created

| File | Purpose |
|------|---------|
| `pyproject.toml` | Package config with requires-python=">=3.12", ruff py312, ty 3.12, full dev toolchain |
| `src/bunny_cdn_sdk/__init__.py` | Empty package root (zero bytes — Phase 2 adds re-exports) |
| `src/bunny_cdn_sdk/_client.py` | Stub for base HTTP client (Phase 3) |
| `src/bunny_cdn_sdk/_exceptions.py` | Stub for exception hierarchy (Phase 1 Plan 02) |
| `src/bunny_cdn_sdk/_pagination.py` | Stub for pagination helpers (Phase 2) |
| `src/bunny_cdn_sdk/_types.py` | Stub for TypedDict definitions (Phase 2) |
| `src/bunny_cdn_sdk/core.py` | Stub for Core API client (Phase 3) |
| `src/bunny_cdn_sdk/storage.py` | Stub for Storage API client (Phase 3) |
| `src/bunny_cdn_sdk/py.typed` | PEP 561 marker (zero bytes) |
| `uv.lock` | Dependency lock for supply chain reproducibility |

### Files Modified

| File | Change |
|------|--------|
| `CLAUDE.md` | Python version constraint updated to 3.12+ |

## Verification Results

1. `grep 'requires-python' pyproject.toml` → `requires-python = ">=3.12"` PASS
2. `grep '3.11' pyproject.toml` → no matches PASS
3. `ls src/bunny_cdn_sdk/` → all 8 entries listed PASS
4. `wc -c src/bunny_cdn_sdk/py.typed` → `0` PASS
5. `uv sync` → exit 0 PASS
6. `uv run python -c "import bunny_cdn_sdk"` → exit 0, no output PASS
7. `uv run ruff check src/` → `All checks passed!` PASS

## Commits

| Hash | Message |
|------|---------|
| `80bf57f` | chore(01-01): update Python version floor to 3.12 |
| `f7e0ffa` | feat(01-01): create module stubs, py.typed marker, and lock file |

## Deviations from Plan

### Auto-fixed Issues

None — plan executed exactly as written.

### Additional Work (Rule 2: Missing Critical Functionality)

**uv.lock committed to version control**
- The threat model (T-01-04) specifies "Commit uv.lock to version control; run pip-audit in CI to detect known-vulnerable transitive deps"
- uv.lock was generated by `uv sync` and committed alongside the module stubs
- Files: `uv.lock`
- Commit: `f7e0ffa`

## Known Stubs

All module files are intentional stubs — they contain only module-level docstrings. This is the expected state per the plan. Each stub will be populated in subsequent phases:

| File | Populated In |
|------|-------------|
| `_client.py` | Phase 2 (Base Client Infrastructure) |
| `_exceptions.py` | Phase 1, Plan 02 |
| `_pagination.py` | Phase 2 |
| `_types.py` | Phase 2 |
| `core.py` | Phase 3 |
| `storage.py` | Phase 3 |
| `__init__.py` | Phase 2 (re-exports) |

These stubs are intentional scaffold — they do not prevent the plan's goal (importable package structure) from being achieved.

## Threat Surface Scan

No new network endpoints, auth paths, file access patterns, or schema changes introduced. The py.typed marker is a zero-byte file by design (T-01-02: accepted). The uv.lock addresses T-01-04 (supply chain).

## Self-Check: PASSED

- pyproject.toml exists: FOUND
- CLAUDE.md updated: FOUND (Python version: 3.12+)
- src/bunny_cdn_sdk/__init__.py: FOUND (0 bytes)
- src/bunny_cdn_sdk/py.typed: FOUND (0 bytes)
- All 6 module stubs: FOUND
- uv.lock: FOUND
- Commit 80bf57f: FOUND
- Commit f7e0ffa: FOUND
